[project]
name = "moltbot-market-data"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "websockets>=12.0",
  "pydantic>=2.7",
]
  "pyarrow>=16.0.0",

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
