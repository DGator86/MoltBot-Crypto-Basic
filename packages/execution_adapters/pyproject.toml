[project]
name = "moltbot-execution-adapters"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "ccxt>=4.3.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
