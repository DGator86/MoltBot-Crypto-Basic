[project]
name = "moltbot-common"
version = "0.1.0"
description = "Common utilities and schemas for Moltbot trader"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.7",
  "pyyaml>=6.0.1",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
