[project]
name = "moltbot-trading-core"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.29.0",
  "pydantic>=2.7",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
